# Convenience entrypoint for local development and CI. The committed lock was
# resolved for Python 3.11 and includes hashes for every distribution.
#
# Source constraints remain in requirements/{base,production,local}.txt.
# Regenerate both locks with:
#   ./scripts/compile-requirements.sh
-r requirements/local.lock.txt

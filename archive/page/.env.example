# Copy to .env and fill in. The Google Sheets API key is SERVER-ONLY: app.py
# proxies /api/sheets/... and the browser never receives the key.
# Restrict the key in Google Cloud (Sheets API only; IP restriction now works
# since only the server calls Google).
SHEETS_API_KEY=your-google-sheets-api-key

// Copy this file to static/config.js and fill in the key (config.js is gitignored).
// This is a CLIENT-SIDE Google Sheets API key: it is exposed to browsers by
// design, so protect it by restricting it in the Google Cloud console
// (HTTP referrers = your domain, API = Google Sheets only).
window.SHEETS_API_KEY = "your-google-sheets-api-key";

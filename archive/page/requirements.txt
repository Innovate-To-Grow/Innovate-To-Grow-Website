# Minimal server: static page serving + server-side Google Sheets proxy
# (the API key stays in .env on the server; the browser never sees it).
Flask==3.1.3
Flask-Caching==2.4.0
python-dotenv==1.2.2
requests==2.34.2
waitress==3.0.2

# Tests
pytest==9.0.3

name: Deploy Frontend to AWS Amplify

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  actions: read
  contents: read

concurrency:
  group: deploy-frontend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy Frontend (${{ matrix.target }})
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: prod
            environment_name: AWS Amplify - Prod
            environment_url: https://i2g.ucmerced.edu
            frontend_url: https://i2g.ucmerced.edu
            api_base_url: https://api.i2g.ucmerced.edu
            backend_proxy_url: https://api.i2g.ucmerced.edu
            proxy_admin_paths: "false"
          - target: demo
            environment_name: AWS Amplify(DEMO) - Prod
            environment_url: https://demo.i2g.ucmerced.edu
            frontend_url: https://demo.i2g.ucmerced.edu
            api_base_url: https://demo.i2g.ucmerced.edu/api
            backend_proxy_url: https://demo-api.i2g.ucmerced.edu
            proxy_admin_paths: "true"
    environment:
      name: ${{ matrix.environment_name }}
      url: ${{ matrix.environment_url }}
    defaults:
      run:
        working-directory: pages

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Validate required deploy configuration
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || secrets.VITE_API_BASE_URL || matrix.api_base_url }}
          AMPLIFY_APP_ID: ${{ vars.AMPLIFY_APP_ID || secrets.AMPLIFY_APP_ID }}
        run: |
          if [ -z "$AMPLIFY_APP_ID" ]; then
            echo "::error::AMPLIFY_APP_ID is required for the Amplify deployment."
            exit 1
          fi
          if [ -z "$VITE_API_BASE_URL" ]; then
            echo "::error::VITE_API_BASE_URL is required for the Amplify build."
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: pages/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || secrets.VITE_API_BASE_URL || matrix.api_base_url }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Configure Amplify rewrite rules (sitemap, API, SPA)
        env:
          AMPLIFY_APP_ID: ${{ vars.AMPLIFY_APP_ID || secrets.AMPLIFY_APP_ID }}
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || secrets.VITE_API_BASE_URL || matrix.api_base_url }}
          AMPLIFY_BACKEND_PROXY_URL: ${{ vars.AMPLIFY_BACKEND_PROXY_URL || matrix.backend_proxy_url }}
          AMPLIFY_PROXY_ADMIN_PATHS: ${{ vars.AMPLIFY_PROXY_ADMIN_PATHS || matrix.proxy_admin_paths }}
        run: |
          python - <<'PY'
          import json
          import os
          import subprocess

          app_id = os.environ["AMPLIFY_APP_ID"].strip()
          api_base_url = os.environ["VITE_API_BASE_URL"].strip().rstrip("/")
          backend_proxy_url = os.environ.get("AMPLIFY_BACKEND_PROXY_URL", "").strip().rstrip("/")
          proxy_admin_paths = os.environ.get("AMPLIFY_PROXY_ADMIN_PATHS", "").strip().lower() in {
              "1",
              "true",
              "yes",
              "on",
          }
          if not app_id:
              raise SystemExit("AMPLIFY_APP_ID is required to configure rewrite rules.")
          if not api_base_url:
              raise SystemExit("VITE_API_BASE_URL is required to configure rewrite rules.")
          if not backend_proxy_url:
              backend_proxy_url = api_base_url

          app_payload = subprocess.check_output(
              ["aws", "amplify", "get-app", "--app-id", app_id, "--output", "json"],
              text=True,
          )
          app = json.loads(app_payload)["app"]
          existing_rules = app.get("customRules") or []

          sitemap_rule = {
              "source": "/sitemap.xml",
              "target": f"{backend_proxy_url}/sitemap.xml",
              "status": "200",
          }
          api_rule = {
              "source": "/api/<*>",
              "target": f"{backend_proxy_url}/<*>",
              "status": "200",
          }
          admin_rules = [
              {
                  "source": "/admin",
                  "target": f"{backend_proxy_url}/admin/",
                  "status": "200",
              },
              {
                  "source": "/admin/<*>",
                  "target": f"{backend_proxy_url}/admin/<*>",
                  "status": "200",
              },
              {
                  "source": "/static/<*>",
                  "target": f"{backend_proxy_url}/static/<*>",
                  "status": "200",
              },
              {
                  "source": "/media/<*>",
                  "target": f"{backend_proxy_url}/media/<*>",
                  "status": "200",
              },
          ]

          # SPA deep-link fallback. The previous "/<*>" rule with status "404-200"
          # did NOT rewrite on the custom domain — hard navigations to client routes
          # (e.g. /past-projects/<id>) fell through to the S3 website endpoint and
          # returned its index.html error document with a 404 status. This is the
          # canonical Amplify SPA rule: any path WITHOUT a static-asset extension is
          # rewritten (status 200) to /index.html so React Router can resolve it.
          # It is placed LAST so the sitemap and /api/<*> passthrough rules win first
          # (extensionless /api/... paths would otherwise match this rule).
          spa_rule = {
              "source": (
                  r"</^[^.]+$|\.(?!(css|gif|ico|jpg|jpeg|js|png|txt|svg|webp|avif|"
                  r"woff|woff2|ttf|otf|eot|map|json|xml|webmanifest)$)([^.]+$)/>"
              ),
              "target": "/index.html",
              "status": "200",
          }

          # Preserve any other existing rules (notably the /api/<*> backend
          # passthrough) but drop the stale sitemap rule and ANY old SPA fallback
          # (identified by target /index.html, e.g. the broken "/<*>" 404-200 rule),
          # so re-running this step is idempotent.
          preserved = [
              rule
              for rule in existing_rules
              if rule.get("source") != sitemap_rule["source"]
              and rule.get("source") != api_rule["source"]
              and rule.get("source") not in {admin_rule["source"] for admin_rule in admin_rules}
              and rule.get("target") != "/index.html"
          ]

          merged_rules = [sitemap_rule, api_rule]
          if proxy_admin_paths:
              merged_rules += admin_rules
          merged_rules += preserved + [spa_rule]

          with open("custom-rules.json", "w", encoding="utf-8") as f:
              json.dump(merged_rules, f)

          subprocess.check_call(
              [
                  "aws",
                  "amplify",
                  "update-app",
                  "--app-id",
                  app_id,
                  "--custom-rules",
                  "file://custom-rules.json",
              ]
          )
          PY

      - name: Deploy to AWS Amplify
        id: deploy-amplify
        env:
          AMPLIFY_APP_ID: ${{ vars.AMPLIFY_APP_ID || secrets.AMPLIFY_APP_ID }}
          AMPLIFY_BRANCH: ${{ vars.AMPLIFY_BRANCH || 'main' }}
        run: |
          # Create a deployment and get the upload URL + job ID
          DEPLOY_RESULT=$(aws amplify create-deployment \
            --app-id "$AMPLIFY_APP_ID" \
            --branch-name "$AMPLIFY_BRANCH")

          JOB_ID=$(echo "$DEPLOY_RESULT" | jq -r '.jobId')
          UPLOAD_URL=$(echo "$DEPLOY_RESULT" | jq -r '.zipUploadUrl')

          echo "Job ID: $JOB_ID"

          # Zip the build output
          cd dist
          zip -r ../build.zip .
          cd ..

          # Upload the zip to the pre-signed URL
          curl -T build.zip "$UPLOAD_URL" \
            -H "Content-Type: application/zip"

          # Start the deployment
          aws amplify start-deployment \
            --app-id "$AMPLIFY_APP_ID" \
            --branch-name "$AMPLIFY_BRANCH" \
            --job-id "$JOB_ID"

          echo "job-id=$JOB_ID" >> "$GITHUB_OUTPUT"
          echo "Deployment started successfully (Job ID: $JOB_ID). Check Amplify console for status."

      - name: Wait for Amplify deployment to finish
        env:
          AMPLIFY_APP_ID: ${{ vars.AMPLIFY_APP_ID || secrets.AMPLIFY_APP_ID }}
          AMPLIFY_BRANCH: ${{ vars.AMPLIFY_BRANCH || 'main' }}
          JOB_ID: ${{ steps.deploy-amplify.outputs.job-id }}
        run: |
          set -euo pipefail

          while true; do
            status="$(aws amplify get-job \
              --app-id "$AMPLIFY_APP_ID" \
              --branch-name "$AMPLIFY_BRANCH" \
              --job-id "$JOB_ID" \
              --query 'job.summary.status' \
              --output text)"
            echo "Amplify job $JOB_ID status: $status"

            case "$status" in
              SUCCEED)
                exit 0
                ;;
              FAILED|CANCELLED)
                aws amplify get-job \
                  --app-id "$AMPLIFY_APP_ID" \
                  --branch-name "$AMPLIFY_BRANCH" \
                  --job-id "$JOB_ID" \
                  --output json
                exit 1
                ;;
              *)
                sleep 15
                ;;
            esac
          done

      - name: Smoke check deployed frontend
        env:
          FRONTEND_URL: ${{ vars.FRONTEND_URL || matrix.frontend_url }}
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || secrets.VITE_API_BASE_URL || matrix.api_base_url }}
        run: |
          set -euo pipefail

          frontend="${FRONTEND_URL%/}"
          api="${VITE_API_BASE_URL%/}"

          for url in "$frontend/" "$frontend/robots.txt" "$frontend/sitemap.xml" "$api/readyz/"; do
            echo "Checking $url"
            curl --fail --show-error --silent --location --max-time 30 "$url" >/tmp/smoke-body
            if [ ! -s /tmp/smoke-body ]; then
              echo "::error::Smoke target returned an empty body: $url"
              exit 1
            fi
          done
